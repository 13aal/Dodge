# -*- encoding: utf-8 -*-
require File.expand_path('../lib/dogeify/version', __FILE__)

Gem::Specification.new do |spec|
  gem.authors       = ["13aal"]
  gem.email         = ["baalisback@yahoo.com"]
  gem.description   = %q{Idk yet I'll let you know when I have it figured out}
  gem.summary       = %q{TODO: Write a gem summary}
  gem.homepage      = ""

  gem.executables   = `git ls-files -- bin/*`.split("\n").map{ |f| File.basename(f) }
  gem.files         = `git ls-files`.split("\n")
  gem.test_files    = `git ls-files -- {test,spec,features}/*`.split("\n")
  gem.name          = "dogeify"
  gem.require_paths = ["lib"]
  gem.version       = Dogeify::VERSION
  spec.add_development_dependency 'rspec'
  spec.add_dependency 'entagger'
end
